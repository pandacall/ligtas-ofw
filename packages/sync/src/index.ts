// Real home of the DMW fetch client / nightly-sync job lands in a later
// ticket. See ../DATA-SOURCES.md for the discovered endpoints.
import { DB_PLACEHOLDER } from "@ligtas-ofw/db";

export const SYNC_PLACEHOLDER = `sync:${DB_PLACEHOLDER}`;
